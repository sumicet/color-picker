import { Slider } from './slider';
import { Tooltip } from './tooltip';
import { Accordion } from './accordion';
import { Input } from './input';

export const components = { Slider, Tooltip, Accordion, Input };
